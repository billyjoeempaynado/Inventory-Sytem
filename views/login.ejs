<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inventory System</title>
  <link rel="stylesheet" href="/output.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>

   
  <form action="/users/login" method="POST">
  <div class="flex items-center justify-center min-h-screen bg-gray-100">
    <div class="bg-white p-8 rounded-lg shadow-md max-w-sm w-full">
        <div class="text-center mb-6">
            <h2 class="text-2xl font-semibold text-gray-800">Login</h2>
        </div>
       <!-- Success Alert -->
       <% if (messages.success_msg) { %>
        <div id="loginSuccessAlert" class="bg-green-100 border border-green-400 text-green-700 px-2 py-1 rounded relative mb-2" role="alert">  
          <span class="block sm:inline"><%= messages.success_msg %></span>         
        </div>
        <% } %>
  
        <!-- Error Alert (if any) -->
        <% if (messages.error) { %>
        <div id="loginErrorAlert" class="bg-red-100 border border-red-400 text-red-700 px-2 py-1 rounded relative mb-2" role="alert">
          <span class="block sm:inline"><%= messages.error %></span>
        </div>
        <% } %>
        
      
        <form action="">
            <div class="mb-4">
                <label class="block text-gray-600 mb-2" for="email">Enter email</label>
                <input class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400" id="email" type="email" name="email" placeholder="Enter email" autocomplete="off" />
            </div>
            <div class="mb-6">
                <label class="block text-gray-600 mb-2" for="password">Enter password</label>
                <input class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400" name="password" id="password" type="password" placeholder="Enter password" />
            </div>
            <input type="submit" value="Login" class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition duration-200"></input>
        </form>
        <p class="mt-4 text-center text-gray-600">Register <a class="text-blue-500 hover:underline" href="/users/register">Click here</a></p>
    </div>
</div>
</form> 

<script type="module" src="/product/ui.js"></script>
</body>
</html>